mixin checkbox-button(id, text, checked, desc)
    .checkbox-button-wrapper(onclick=`onCheckboxClick(${id})`)
        .checkbox-button(id=id, class=checked ? 'checked' : '')
            .checkbox-button__mark(class=checked ? 'checked' : '')
        p= text

